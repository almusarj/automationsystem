# Torquehouse frontend

React app for the garage management system. See the root `README.md` for full
setup and deployment instructions.

Quick start:
```bash
npm install
cp .env.example .env
npm run dev
```
